# Path to the directory containing events.db and acestep_status.json
# This is written by your OpenClaw scripts (log_event.py, check_acestep.py)
OPENCLAW_MONITOR_DIR=/root/.openclaw/monitor

# Port to expose the dashboard on
DASHBOARD_PORT=8080
